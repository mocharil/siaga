# Devlog — 30 Agustus 2026

## Ringkasan Eksekusi: Tugas 0, D1, D2, D3, D4 & Persiapan Deliverable

Hari ini seluruh eksekusi pengembangan antarmuka pemantauan lokal (Local-First Security Dashboard D1–D4) dan perbaikan integritas metrik diselesaikan dan diuji secara menyeluruh.

---

### 1. Tugas 0: Perbaikan Integritas Metrik `/api/metrics` (Kepatuhan CLAUDE.md #2)
* **Masalah:** Endpoint `/api/metrics` sebelumnya memuat nilai statis (hardcoded) yang menyalin angka ilustratif dari rencana (misalnya `avg_lead_time_hours: 31.0`). Hal ini berisiko menyamarkan hasil evaluasi model yang sesungguhnya.
* **Solusi:**
  1. Metrik evaluasi model AI (`precision_pct`, `recall_pct`, `f1_score`, `eval_timestamp`) kini dibaca secara dinamis dari berkas `data/eval_results.json` hasil eksekusi nyata `scripts/run_eval.py`.
  2. Jika berkas evaluasi belum tersedia, sistem mengembalikan nilai `null` dengan flag `"metrics_available": false` dan status `"uncalibrated"`, bukan mengarang angka contoh.
  3. Metrik *Lead Time* dihitung dari selisih waktu nyata antara `first_seen` dan `blacklist_listed_at` pada tabel `domain_findings`. Jika belum ada domain terdeteksi yang kemudian masuk feed publik, sistem secara jujur mengembalikan `null` dengan catatan `"belum cukup data"`.
  4. Status kalibrasi diturunkan secara dinamis dari tanggal evaluasi terakhir: `"calibrated (YYYY-MM-DD)"`.

---

### 2. Tugas D2 & D3: Dashboard UI Statis & Grafik Tren Inline SVG
* **Vanilla Stack Tanpa Build Step:** Dibangun menggunakan HTML5, CSS3, dan Vanilla JavaScript murni tanpa build tool (tanpa npm, webpack, atau Vite) sehingga deployment ke VPS Linux cukup dengan menyalin direktori `dashboard/static/`.
* **Zero External CDN Dependencies (100% Offline-Ready):** Tidak ada skrip eksternal (tanpa Chart.js, D3.js, atau CDN font) sehingga tampilan dashboard tahan terhadap kegagalan jaringan saat perekaman video presentasi.
* **Grafik Inline SVG:** Komponen grafik tren 14 hari dan visualisasi efisiensi penyaringan bertingkat (*funnel ratio bars*) digambar langsung secara matematis via elemen `<svg>` inline (~35 baris JavaScript).
* **Kepatuhan Privasi & UU PDP (Default Domain Masking):** Nama domain temuan disamarkan secara otomatis di tabel UI (contoh: `p***.web.id`, `c***.shopee.co.id...`) guna mencegah potensi pencemaran nama baik (*defamation*) pada rekaman video. Disediakan toggle *Penyamaran* dengan status default **AKTIF**.

---

### 3. Tugas D4: Pengemasan & Pengukuran Efisiensi RAM
* **Skrip Peluncuran Mandiri:**
  * Linux / VPS: `dashboard/run.sh`
  * Windows PowerShell: `dashboard/run.ps1`
* **Pengukuran RAM Aktual:**
  * Konsumsi memori terukur pada proses FastAPI/Uvicorn yang melayani *real traffic*: **63.79 MB RSS** (jauh di bawah batas anggaran VPS 200 MB).
* **Hardening Panduan Deploy VPS (T09):**
  * Dashboard dikonfigurasi untuk hanya bind pada `127.0.0.1:8000`. Akses dari luar VPS wajib menggunakan **SSH Port Forwarding / Tunnel** (`ssh -N -L 8000:127.0.0.1:8000 user@vps-ip`). Port 8000 dilarang dibuka ke publik (`0.0.0.0`).
* **Tangkapan Layar:** Disimpan di `docs/dashboard_screenshot.png` sebagai bahan artikel dan lampiran deliverable.

---

### 4. Tugas T27: Draf Laporan Terstruktur & Verifikasi Kanal Resmi
* **Modul Laporan:** Mengimplementasikan `lib/report_draft.py` dan CLI `scripts/draft_report.py` untuk menyusun draf laporan insiden terstruktur dengan bukti teknis lengkap (RDAP, DNS, HTTP trace, status blacklist).
* **Verifikasi Kontak Resmi (Anti-Fabrication):** Seluruh kontak resmi diverifikasi langsung dari situs web resmi masing-masing instansi:
  * **BSSN Gov-CSIRT:** `bantuan70@bssn.go.id`, Telp `(021) 78833610`, WhatsApp `0812-8135-4598` (24/7) (Sumber: bssn.go.id/aduan-siber).
  * **Kominfo Aduan Konten:** `aduankonten@kominfo.go.id`, WhatsApp `08119224545`, portal `aduankonten.id` (Sumber: aduankonten.id/kontak-kami).
  * **OJK 157 & Satgas PASTI:** `konsumen@ojk.go.id`, `satgaspasti@ojk.go.id`, Telp `157`, WhatsApp `081157157157`, portal `kontak157.ojk.go.id`.
  * **PANDI:** `abuse@pandi.id`, `helpdesk@pandi.id`, Telp `+62-21-30055777`, portal IDADX `https://idadx.id/report` (Sumber: pandi.id/kontak).
* **Human-in-the-Loop Enforced:** Skrip ini secara tegas HANYA menyusun draf teks, tanpa mekanisme pengiriman otomatis untuk menjamin verifikasi operator manusia.

---

### 5. Tugas 1: Pemrosesan Ulang Backlog Lengkap & Deteksi Kampanye Nyata
* **Eksekusi Pipeline Terkalibrasi:** Seluruh data `ct_raw` dari semua tanggal historis diproses ulang melalui `scripts/run_pipeline.py --allow-network`:
  * `2026-08-30`: 9.125 domain dipindai ➔ 310 Tahap 1 ➔ 310 Tahap 2 ➔ 3 temuan berbobot.
  * `2026-08-29`: 6.522 domain dipindai ➔ 249 Tahap 1 ➔ 247 Tahap 2 ➔ 1 temuan berbobot.
  * `2026-08-28`: 13.534 domain dipindai ➔ 515 Tahap 1 ➔ 515 Tahap 2 ➔ 7 temuan berbobot.
* **Hasil Deteksi Kampanye Nyata:**
  * **Nameserver Cluster:** 1 kluster nameserver terdeteksi mencakup 3 domain.
  * **Brand Pattern Cluster:** 48 kluster pola peniruan merek terdeteksi pada basis data domain temuan (misalnya peniruan `shopee indonesia`, `investree indonesia`, `paxel indonesia`, `bank nagari`, `bank kalteng`, `bank negara indonesia`).
* **Deprecation Notice:** Berkas usang `scripts/rescore_backlog.py` diberi catatan arsitektur jelas bahwa `lib/pipeline.py` adalah satu-satunya jalur resmi dan terkalibrasi.

---

### 6. Tugas 2: Profiling & Optimasi Performa `find_similar()` (14.8x Lebih Cepat)
* **Hasil Profiling Baseline (cProfile):**
  * Baseline awal: **22.55 ms/domain** (45.10 detik untuk 2.000 domain).
  * Bottleneck utama: komputasi matriks `damerau_levenshtein_distance` (173.086 panggilan untuk 500 domain pada Teknik 4 yang membandingkan setiap label tanpa filter panjang/karakter).
* **Solusi & Optimasi:**
  1. *Early length exit*: Pengecekan `abs(len(label) - len(stem)) > max_dist` langsung keluar sebelum alokasi matriks.
  2. *Set overlap pre-filtering*: Pra-komputasi `set(label)` satu kali per domain label untuk menyaring kandidat sebelum DL.
  3. Pre-komputasi stem length dan stem set pada struktur `WatchlistEntry`.
* **Hasil Pengukuran Ulang:**
  * Kecepatan baru: **1.51 ms/domain** (3.03 detik untuk 2.000 domain).
  * **Peningkatan Performa: 14.8x LEBIH CEPAT.**
  * **Integritas Akurasi:** 100% identik (33 kecocokan pada sampel uji sebelum == 33 kecocokan sesudah, 0 regresi).

---

### 7. Tugas 3: Skrip Hardening Sistem Operasi VPS (`scripts/vps_hardening.sh`)
* **Spesifikasi Keamanan:**
  * Pembuatan user non-root `siaga` dengan hak sudo.
  * Hardening SSH Key-Only (`PasswordAuthentication no`, `PermitRootLogin no`, `MaxAuthTries 3`).
  * Firewall UFW default DENY incoming, hanya membuka SSH (port 22) dan mengisolasi port monitoring (FastAPI/OpenClaw) di `127.0.0.1`.
  * Integrasi `fail2ban` dan pembaruan keamanan otomatis via `unattended-upgrades`.
* **Verifikasi:** Lolos verifikasi sintaksis `bash -n scripts/vps_hardening.sh`.

---

### 8. Tugas 4: Draf Artikel Lomba (`docs/article_draft.md`)
* **Target Kata:** 1.350 kata (dalam rentang sasaran 1.200–1.500 kata).
* **Struktur:** 8 bagian lengkap sesuai panduan `plan/07-deliverables.md`.
* **Transparansi & Pembelajaran Nyata:** Mendokumentasikan bug riil yang ditemukan (selisih zona waktu UTC vs WIB, hardcoding metrik vs kalibrasi dinamis, verifikasi sitasi kontak resmi, dan komitmen privasi UU PDP No. 27/2022).
* **Backlink Wajib:** Penandaan placeholder `[Cloud VPS]` dan `[AI Hosting]` secara kontekstual di bagian arsitektur dan operasional server.

---

### 9. Status Akhir Keseluruhan Test Suite
* **186 tests lulus dari 186 tests (100% Green, 0 failed) dalam 49.90 detik.**
